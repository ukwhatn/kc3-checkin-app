<% content_for(:title, "タイムテーブル編集") %>
<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= render partial: "admin/timetables/components/form", locals: { model: @timetable, path: admin_event_timetable_path(@event, @timetable) } %>
    </div>
  </div>
</div>