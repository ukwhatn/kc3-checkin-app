<% content_for(:title, "新規登録") %>

<div class="container-fluid">
  <div class="row">
    <div class="col-12 text-center">
      <div>
        以下の情報で新規登録を行います
      </div>
      <div class="my-2" style="font-size: 75%">
        修正する場合は「戻る」ボタンを押してください
      </div>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-md-6">
      <table class="table mx-auto my-3 table-light">
        <tbody>
        <tr>
          <th>ユーザー名</th>
          <td><%= @new_user.full_name %></td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td><%= @new_user.email %></td>
        </tr>
        <tr>
          <th>所属大学</th>
          <td><%= @new_user.univ.name %></td>
        </tr>
        <tr>
          <th>学年</th>
          <td><%= @new_user.grade_i18n %></td>
        </tr>
        <tr>
          <th>所属団体</th>
          <td><%= @new_user.group.name %></td>
        </tr>
        </tbody>
      </table>
      <div class="container-fluid">
        <div class="row">
          <div class="col-6">
            <%= link_to '戻る', user_registration_path, class: 'btn btn-outline-secondary btn-block d-block mx-auto' %>
          </div>
          <div class="col-6">
            <%= link_to '登録', user_registration_path, method: :post, class: 'btn btn-primary btn-block d-block mx-auto', data: { turbo_method: :post } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
    history.replaceState('', '', '/user/registration/confirm');
</script>